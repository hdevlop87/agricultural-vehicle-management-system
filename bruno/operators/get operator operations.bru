meta {
  name: get operator operations
  type: http
  seq: 8
}

get {
  url: {{baseUrl}}/operators/{{operatorId}}/operations
  body: none
  auth: inherit
}

headers {
  authorization: Bearer {{token}}
}
