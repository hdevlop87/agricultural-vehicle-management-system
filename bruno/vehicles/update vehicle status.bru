meta {
  name: update vehicle status
  type: http
  seq: 18
}

put {
  url: {{baseUrl}}/vehicles/{{vehicleId}}/status
  body: json
  auth: inherit
}

headers {
  authorization: Bearer {{token}}
}

body:json {
  {
    "status": "active"
  }
}
