meta {
  name: get fields with active operations
  type: http
  seq: 11
}

get {
  url: {{baseUrl}}/fields/active-operations
  body: none
  auth: inherit
}

headers {
  authorization: Bearer {{token}}
}
